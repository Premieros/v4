# معايير وقواعد العمل المعمارية الإلزامية للمشروع (POS / ERP)

## 1. قاعدة العمل الأساسية

اعتمد فقط على:
* الملفات الموجودة محليًا.
* حالة Git المحلية.
* الفرع المحلي الحالي.
* الاختبارات المحلية.
* الملفات التي يحددها المستخدم.
* سجل العمل الموجود داخل المشروع.

لا تفترض أي شيء عن GitHub Remote.
لا تقل إن التعديل موجود على GitHub أو تم Push إلا إذا قام المستخدم بذلك بنفسه أو أعطاك دليلًا واضحًا.

---

## 2. ممنوع Push أو Merge تلقائي

ممنوع تنفيذ:
* git push
* git merge
* git rebase على فروع بعيدة
* Pull Request
* GitHub Actions modifications
* Production deployment

إلا بطلب صريح من المستخدم.
الـ Push يتم يدويًا بواسطة المستخدم.

بعد انتهاء العمل فقط أخبر المستخدم:
* ما الملفات التي تغيرت.
* ما الاختبارات التي نجحت.
* ما الـ commit المقترح.
* أن التغييرات جاهزة للـ Push اليدوي.

---

## 3. افحص الموجود قبل إضافة أي شيء

قبل أي تعديل:
1. ابحث عن الميزة المطلوبة داخل المشروع.
2. افحص Frontend.
3. افحص Services.
4. افحص RPCs.
5. افحص الجداول والـ migrations.
6. افحص الاختبارات المرتبطة.
7. افحص Permissions و RLS.
8. حدد هل المطلوب:
   * موجود كاملًا.
   * موجود جزئيًا.
   * موجود لكنه معطل.
   * غير موجود.

ثم نفذ الناقص فقط.

---

## 4. لا تعيد بناء الموجود

إذا وجدت تنفيذًا قائمًا ويعمل:
* لا تعيد كتابته.
* إذا كان ناقصًا: أكمل الناقص فقط.
* إذا كان به Bug مثبت: أصلح الـ Bug فقط.
* إذا كان موجودًا تحت اسم مختلف: استخدم الموجود ولا تنشئ نسخة ثانية.

---

## 5. لا توسع نطاق المهمة

اعمل فقط على المطلوب الحالي.
ممنوع أثناء إصلاح ميزة واحدة:
* إعادة تصميم النظام كاملًا.
* Refactor واسع غير ضروري.
* تغيير ملفات غير مرتبطة.
* تنظيف شامل للكود بدون طلب.
* تحديث مكتبات بدون ضرورة.
* تغيير تصميم شاشات أخرى.
* تعديل قاعدة البيانات بلا حاجة مثبتة.

أقل تعديل صحيح هو المطلوب.

---

## 6. المرجع المعماري الإلزامي

النظام POS/ERP للمطاعم.
الهدف النهائي هو نظام احترافي متعدد الفروع والمخازن، Permission-First، مع عزل كامل للبيانات.

---

## 7. الصلاحيات Permission-First

### القاعدة الأساسية:
* **Permission** تحدد: ماذا يستطيع المستخدم أن يفعل.
* **Branch Access** تحدد: في أي فرع يستطيع تنفيذ ذلك.
* وجود أحدهما لا يمنح الآخر، ويجب تحقق الاثنين معًا دائمًا: `Permission + Branch Access`.

### Super Admin:
* Super Admin فقط يملك Full Access ضمنيًا.

### باقي الأدوار:
مثل (`owner`, `branch_manager`, `cashier`, `accountant`, `warehouse_manager`, `kitchen`, أو أي Custom Role):
* هي Labels/Templates فقط.
* لا تستخدم اسم الدور كبديل عن Permission.
* ممنوع كتابة منطق مثل: `if role == owner then allow everything` إلا إذا كان هذا القرار مثبتًا صراحة في معمارية المشروع.

---

## 8. المستخدم متعدد الفروع

المستخدم يمكن أن يعمل على:
* فرع واحد.
* عدة فروع.
* كل الفروع الممنوحة له.

لا تفترض أن المستخدم تابع لفرع واحد فقط.

### Primary Branch:
* يمكن وجود `primary_branch_id` فقط كفرع افتراضي عند تسجيل الدخول.
* لا يجوز استخدامه كحد أمني أو كنطاق وحيد للمستخدم.

### Multiple Branch Access:
* يجب أن يدعم النظام علاقة مستقلة بين المستخدم والفروع، مثل `user_branch_access` أو أي جدول/معمارية موجودة تؤدي نفس الغرض (مثال: `user_id | branch_id`).
* شرط الوصول لفرع:
  1. أن يكون لديه Branch Access فعلي لهذا الفرع.
  2. أن تكون لديه Permission المطلوبة للعمل.
* ممنوع الاعتماد على `users.branch_id` وحده لتحديد نطاق المستخدم.

---

## 9. Branch Selector

إذا كان لدى المستخدم أكثر من فرع:
* يجب إظهار Branch Selector.
* عند اختيار فرع: كل شاشات وبيانات العمل يجب أن تستخدم الفرع النشط المحدد (POS, Orders, Products, Components, Raw Materials, Inventory, Warehouses, Customers, Suppliers, Purchases, Expenses, Sales, Shifts, Reports, KDS, Approvals, Transfers, Waste, Accounting).
* لا يجوز خلط بيانات أكثر من فرع في عملية تشغيلية واحدة إلا إذا كانت العملية مصممة Cross-Branch (مثل Warehouse Transfer مصرح به).

---

## 10. إدارة مستخدم متعدد الفروع

من شاشة المستخدم يجب دعم:
* تحديد Primary Branch.
* منح الوصول لفرع واحد أو عدة فروع.
* إضافة فرع للمستخدم أو إزالة فرع منه.
* عرض الفروع المسموح بها.
* الحفاظ على المستخدم عند إزالة وصوله من فرع.
* إذا كان المدير يدير المستخدمين: لا يمكنه منح المستخدم فرعًا لا يملك هو نفسه صلاحية إدارته، إلا إذا كان Super Admin.

---

## 11. عزل الفروع (Branch Isolation)

كل بيانات الأعمال يجب أن تحترم Branch Isolation. لا تسمح بقراءة أو تعديل بيانات فرع غير مصرح للمستخدم به.

---

## 12. RLS (Row Level Security)

RLS يجب أن يكون الحماية الأساسية على مستوى قاعدة البيانات:
* المستخدم يمكنه الوصول إلى السجل فقط إذا كان Super Admin أو لديه Branch Access للـ `branch_id` الخاص بالسجل مع الصلاحية المناسبة.
* ممنوع تعطيل RLS، أو إضعاف Policies، أو إضافة bypass عام.
* ممنوع الاعتماد على Frontend فقط، أو منح وصول شامل لمجرد اسم الدور، أو استخدام `SECURITY DEFINER` لمجرد حل Permission Error.
* أي مشكلة RLS يجب حل سببها الحقيقي.

---

## 13. المنتجات والمكونات والخامات والوحدات

تعامل مع هذه الكيانات كلٌ على حدة:
* **Products**: المنتجات التي تباع أو تشترى أو تخزن.
* **Product Components**: المكونات المرتبطة بتركيبة المنتج.
* **Raw Materials**: الخامات المستقلة المستخدمة في الوصفات والاستهلاك.
* **Units**: وحدات القياس والتحويل.
* **Warehouses**: المخازن.
* **Inventory**: الرصيد والحركات.
لا تخلط بينها وافحص القائم قبل إنشاء جداول أو خدمات جديدة.

---

## 14. POS

يجب الحفاظ على دعم:
* Dine-in, Take Away, Delivery, Drive Thru, Quick Order.
* Tables, Hold / Resume, Send to Kitchen, KDS.
* Payment, Split Payment, Discount, Void, Cancel, Reprint.
* Shift operations.

---

## 15. قاعدة Send to Kitchen

* يتم خصم المخزون عند `Send to Kitchen`.
* الخصم يجب أن يكون: **Delta-only** و **Idempotent** وغير قابل للتكرار لنفس الكمية (إذا تم إرسال 2 ثم أضيف 1، يخصم 1 فقط).

---

## 16. الدفع

* إذا تم خصم المخزون عند `Send to Kitchen`، ممنوع خصمه مرة أخرى عند `Payment`.
* يجب منع **Double Inventory Deduction**.

---

## 17. Void / Cancel

* إذا تم إلغاء كمية سبق إرسالها للمطبخ وتم خصم مخزونها: يجب استرجاع الكمية الصحيحة حسب الحالة بدون استرجاع مضاعف.
* كل استرجاع يجب أن يكون مسجلًا ومحدد المصدر.

---

## 18. KDS

* KDS يستقبل Delta فقط.
* لا ترسل نفس الكمية أكثر من مرة، ويجب وجود سجل واضح للكمية التي سبق إرسالها.

---

## 19. الورديات (Shifts)

Cashier يجب أن يعمل داخل Shift صحيحة إذا كانت الوردية إلزامية، مع دعم:
* Open Shift, Opening Drawer.
* Sales, Returns, Discounts, Voids.
* Cash / Card totals, Drawer expected vs actual.
* Close Shift, Day Close.

---

## 20. Guided Routing

لا تعرض Error تقنيًا للمستخدم إذا كان ينقصه Setup إلزامي يمكن توجيهه إليه:
* لا يوجد Branch -> Branch Selector أو إعداد الفروع.
* لا يوجد Warehouse -> إعداد المخزن.
* لا توجد Shift -> Open Shift.
* لا توجد Products -> شاشة المنتجات.
* لا توجد Units -> إعداد Units.
* لا يملك Branch Access -> اعرض رسالة صلاحية واضحة ولا تتجاوز RLS.

---

## 21. التقارير

* صفحة Reports موحدة بدون رسوم بيانية إلا بطلب صريح.
* تعتمد على: Tables, Filters, Totals, Date range, Branch, Multi-Branch filter, Employee, Payment Method, Product, Order Type, Export Excel, Custom Columns.
* فلتر الفروع يعرض فقط الفروع الممنوحة للمستخدم، و Super Admin فقط يرى كل الفروع.

---

## 22. واجهة المستخدم

* العربية RTL هي الأولوية (Sidebar يمين عند العربية، يسار عند الإنجليزية، قابل للطي).
* الواجهة: بسيطة، احترافية، Touch Friendly، Responsive ومناسبة لنقاط البيع.
* لا تجعل تعديل مكان عنصر يؤثر على منطق العمل.

---

## 23. الاختبارات

بعد كل تعديل شغّل الاختبارات المتعلقة بالمهمة، وعند إغلاق مرحلة مهمة:
* lint, typecheck, unit, build, Fresh DB, schema verification, integration, security/RLS, browser smoke.
* إضافة اختبارات لـ Multiple Branch Access تشمل سيناريوهات الصلاحيات المختلفة ومنع Cross-Branch leakage.
* لا تحذف اختبارًا ناجحًا سابقًا، ولا تغير Expected Result لإخفاء Bug.

---

## 24. قاعدة البيانات

* لا تطبق أي Migration على Production تلقائيًا.
* لا تفترض أن Migration موجودة في قاعدة البيانات لأنها موجودة محليًا.
* إذا لم تستطع الاتصال بقاعدة البيانات اكتب بوضوح: `غير متحقق من قاعدة البيانات` ولا تعتبرها PASS.

---

## 25. Git المحلي

قبل بدء أي مهمة اعرض:
* Branch الحالي، git status، آخر commit محلي، والملفات غير الملتزم بها.
* إذا وجدت تعديلات سابقة غير مفهومة: لا تحذفها، وحافظ عليها.

---

## 26. التزامن مع نموذج آخر

* لا تفترض أنك الوحيد الذي يعدل الملفات.
* اقرأ أحدث نسخة محلية قبل التعديل.
* ممنوع: `git reset --hard`, `force checkout`, حذف ملفات غير مفهومة، أو الكتابة فوق عمل نموذج آخر.

---

## 27. بعد كل مهمة

اكتب تقريرًا مختصرًا يحتوي:
* **الموجود قبل العمل**: ما كان موجودًا بالفعل.
* **ما تم تعديله**: الملفات والتغييرات فقط.
* **ما لم يتم لمسه**: الأنظمة التي تعمدت عدم تغييرها.
* **الاختبارات**: PASS / FAIL.
* **المشاكل المتبقية**: إن وجدت.
* **Git**: التغييرات جاهزة محليًا فقط (لا تقل إنها على GitHub).
* **Push**: انتظر المستخدم ليقوم بالـ Push اليدوي.

---

## 28. قاعدة التوقف

توقف فورًا واكتب للمستخدم سبب التوقف إذا اكتشفت أن المطلوب:
* يتعارض مع قرار معماري سابق.
* سيضعف RLS أو يكسر Branch Isolation.
* سيمنح مستخدمًا وصولًا لفروع غير مصرح بها.
* سيعيد استخدام users.branch_id كحد وحيد.
* سيؤدي إلى Double Inventory Deduction.
* سيكرر ميزة موجودة، أو يحتاج تغييرًا واسعًا غير متوقع، أو يحتاج Production Migration.

---

## القاعدة الذهبية

1. افهم الموجود أولًا.
2. احترم الموجود السليم.
3. أصلح الخلل المثبت فقط.
4. أكمل الناقص فقط.
5. لا تكرر.
6. لا توسع المهمة.
7. لا تلمس Production.
8. لا تعتبر أي شيء موجودًا على GitHub لأنك تعمل محليًا فقط.

> **القاعدة الإلزامية للمستخدم متعدد الفروع:**
> * **Permission** تحدد ماذا يفعل المستخدم.
> * **Branch Access** تحدد أين يفعل ذلك.
> * **Primary Branch** مجرد Default وليس قيدًا أمنيًا.
> * **Super Admin** فقط يملك كل الفروع ضمنيًا.
> * كل مستخدم آخر يمكنه العمل على أي عدد من الفروع الممنوحة له، بشرط `Permission + Branch Access + RLS`.
